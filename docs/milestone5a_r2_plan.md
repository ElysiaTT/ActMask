# Milestone 5A-R2 plan

R2 may only load existing 4R-v3 checkpoints and run frozen-data inference to
store per-example seed scores. It cannot retrain or substitute checkpoints from
another milestone. Missing matching checkpoints is a mandatory inventory stop.
