# Milestone 5A plan

5A first audits the frozen 4R-v3 centroid result and report aggregation. If
the shortcut is fair, it probes two new state-only relational mechanisms whose
counterfactual pairs exactly match global centroid, covariance, speed and
unordered-token moments. No visual data or final model is permitted before the
predeclared state gate passes.
