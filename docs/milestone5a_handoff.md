# Milestone 5A handoff

All 5A output is isolated under `outputs/actmask/milestone5a_relational_probe/`.
Frozen 4R-v3 output is only read for forensics and non-regression checks.
5A confirms the centroid shortcut is fair, but finds a seed-average versus
ensemble-bootstrap CI estimand mismatch in frozen reporting. The raw report
and decision remain unmodified; see `metric_aggregation_audit.json` and
`corrected_4r_v3_reporting_note.json` before resuming relational task design.
