# Milestone 5B-Data-F plan

This is a read-only saturation forensic. It inventories the repaired probe,
performs checkpoint-only cue/token perturbations for GRU and TCN, assesses
whether proposed held-out splits are valid for existing IID checkpoints, and
recommends the smallest benchmark repair. It does not generate simulation data
or train a new method.
