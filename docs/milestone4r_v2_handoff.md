# Milestone 4R-v2 handoff

4R-v2 is isolated at `outputs/actmask/milestone4r_v2_candidate_diversity/`. The frozen 4R files were not modified. The approved no-render preprobe and full visual generation are complete, with full evidence in `preprobe_attempt_1` and `full_probe`.

The placement repair itself is valid, but full v2 is decision E because the two unmodified families fail the new all-family diversity requirement. Do not run any baseline or model from these files. A later effort would need a newly authorized, separately preregistered repair of those other families; it must not alter 4R or retrofit this v2 decision.
