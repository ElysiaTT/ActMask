# Security and safety

This candidate contains no network service and no instruction to operate hardware. Treat all simulator outputs as research artifacts, not safety guarantees. Do not connect a trained verifier to a physical robot without a separate safety review, perception validation, action constraints, and real-world testing. Avoid exposing hidden metadata when creating derivative datasets; doing so invalidates the fair comparison.
