\section{Leakage and validity audits}
The 3S red-team audit checks hidden fields and object IDs in model inputs, candidate template/index rates, degenerate worlds, OOD overlap, common success definition, validation selection, confidence-interval binding, labels, and grouped splits. It reports zero blocking failures and one non-blocking bookkeeping warning. The full candidate-diversity result has 1.0 nondegenerate-world fraction and 0 index-leakage maximum. The valid early-frame pair-swap is used for causal tests; generic reversal is retained only as an invalid diagnostic.
