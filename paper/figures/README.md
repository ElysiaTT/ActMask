# Figure integration

`main.tex` references PNG figures directly from the frozen 3S package via `\graphicspath`; this directory deliberately does not duplicate binary artifacts. Exact figure provenance is in `outputs/actmask/milestone3s_paper_package/figures/figure_provenance.json`.
